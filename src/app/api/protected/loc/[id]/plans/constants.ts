// Shared constants for member plan status definitions

export const ACTIVE_SUBSCRIPTION_STATUSES = ["active", "trialing", "past_due", "paused"] as const;
export const ACTIVE_PACKAGE_STATUSES = ["active", "incomplete"] as const;
